{
  "name": "weei-used-types",
  "version": "1.0.14",
  "description": "Weei types!",
  "scripts": {
    "publish": "npm version patch && git -c credential.helper= -c core.quotepath=false -c log.showSignature=false push --progress --porcelain origin refs/heads/master:master && npm publish"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ]
}
